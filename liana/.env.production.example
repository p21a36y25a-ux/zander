NODE_ENV=production

PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=12h

DB_HOST=mysql
DB_PORT=3306
DB_NAME=liana_hr
DB_USER=liana
DB_PASSWORD=change-this-db-password
MYSQL_ROOT_PASSWORD=change-this-root-password

# Use migrations in production.
DB_SYNC=false

# When the frontend is served behind the same Nginx host, leave these as-is.
VITE_API_URL=/api
VITE_SOCKET_URL=
